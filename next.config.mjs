/** @type {import('next').NextConfig} */
const nextConfig = {
      images: {
            unoptimized: true,
      }
      // experimental:{
      //       cache: true,
      // }
};


export default nextConfig;
